{
  "name": "@tms/post-to-instagram",
  "version": "1.0.0",
  "main": "dist/index",
  "types": "dist/index",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "npm run clean && npm run compile",
    "clean": "rm -rf ./dist",
    "compile": "tsc -p tsconfig.build.json"
  },
  "dependencies": {
    "playwright": "^1.11.1"
  },
  "devDependencies": {
    "typescript": "^4.2.4"
  }
}
