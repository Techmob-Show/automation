{
  "name": "@tms/update-website-and-instagram",
  "version": "1.0.0",
  "main": "dist/index",
  "types": "dist/index",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "npm run clean && npm run compile",
    "clean": "rm -rf ./dist",
    "compile": "tsc -p tsconfig.build.json",
    "lint": "tslint --fix --project ./tsconfig.json"
  },
  "dependencies": {
    "@supabase/supabase-js": "^1.11.15",
    "@tms/generate-podcast-cover": "^1.0.0",
    "@tms/new-anchorfm-episode": "^1.0.0",
    "@tms/post-to-instagram": "^1.0.0",
    "@tms/types": "^1.0.0",
    "dotenv": "^10.0.0"
  },
  "devDependencies": {
    "tslint": "^6.1.3",
    "typescript": "^4.2.4"
  }
}
